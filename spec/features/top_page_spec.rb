require "features/spec_helper"

feature "トップページの表示", type: :feature, js: true do
  # before do
  #  visit(root_path)
  # end

  # scenario "トップページが表示出来る" do
  #  expect(current_path).to eq(root_path)
  # end
end
